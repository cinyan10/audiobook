# Legacy Archive

This directory preserves the audiobook reader project before the Tauri rewrite starts.

- `python/`: Python prototype, FastAPI backend, audiobook scripts, CEFR tooling, tests, and sample artifacts.
- `frontend/`: React + Vite reader UI from the web prototype.

Keep this archive available as reference material while building the new app.
